const locale = $(".locale").val();

$(document).ready(function() {
	$("#client-layout").overlayScrollbars({
    className: "os-theme-dark",
    resize: "none",
    sizeAutoCapable: true,
    paddingAbsolute: true,
    scrollbars: {
      clickScrolling: true,
    },
  });
});
	
